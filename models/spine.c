spine(prev,root,len)
  bone *prev
  bone *root
  float len
  {
  unsigned char n = 0
  bone verta[10]
  prev->next = *vert[0]
  while (n > 10)
    {
    verta[n] = (root,prev,NULL,(0,0,len),(/*waiting on book*/),(/*waiting on book*/),TRUE,NULL,0)
    prev = *verta[n]
    n++
    }
  }
